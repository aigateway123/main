# Release Note — P1 Iteration #008

## Provider 双协议原生直连（OpenAI + Anthropic 双端点）

Version: v1.6.0

Date: 2026-09-10

Scope: feature / breaking change

Status: Ready for Release（代码完成，**尚未部署**）

Owner: Release Manager

---

## 1. Release Summary

| 字段 | 值 |
|------|-----|
| 迭代编号 | P1 Iteration #008 |
| 迭代名称 | Provider 双协议原生直连 |
| 迭代类型 | Feature（含行为变更） |
| 计划上线日期 | 待定（代码已就绪） |
| 当前状态 | 未提交、未部署 |
| 设计文档 | `.trae/documents/Provider-双协议原生直连-方案.md`（v1.0 Approved） |
| 测试报告 | [TEST-20260910-DualProtocol-Native.md](../03-test/TEST-20260910-DualProtocol-Native.md) |
| 数据库变更 | 迁移 `20260910_014_add_provider_anthropic_endpoint`（up/down 已就绪，已在真实 PostgreSQL 验证） |
| 前置 | P1 Iteration #006（Anthropic Messages 端点）、#007（Anthropic SDK 兼容） |

---

## 2. 功能范围

### 2.1 Provider 双协议端点配置【P0】

| # | 功能 | 状态 |
|:-:|------|:----:|
| 1 | `providers` 表新增 `anthropic_base_url` / `anthropic_api_path` / `anthropic_api_key_ref` / `anthropic_auth_type` 四列（均可空） | ✅ |
| 2 | 字段语义重定义：`base_url` / `api_path` / `api_key_ref` / `auth_type` = **OpenAI 端点**；`anthropic_*` = **Anthropic 端点**（`auth_type` 对 OpenAI 固定为 Bearer） | ✅ |
| 3 | 至少需配置一种协议端点，否则创建/更新返回 400 `VALID001` | ✅ |
| 4 | `protocol_type` 保留（展示/兼容用），改由后端按端点推导，不再参与路由决策 | ✅ |
| 5 | 存量迁移：`protocol_type='anthropic'` 的 Provider 端点自动搬运至 `anthropic_*` 列 | ✅ |
| 6 | Admin Provider 表单拆分为 OpenAI / Anthropic 两个端点区，移除「协议类型」下拉；列表新增协议徽标与双端点展示 | ✅ |

### 2.2 按入站路径原生直连【P0】

| # | 功能 | 状态 |
|:-:|------|:----:|
| 1 | `/v1/chat/completions` 入站 → 只选用配置了 OpenAI 端点的 Provider，以 OpenAI 协议出站 | ✅ |
| 2 | `/v1/messages` 入站 → 只选用配置了 Anthropic 端点的 Provider，以 Anthropic 协议出站 | ✅ |
| 3 | 请求体、响应体、流式 SSE、4xx 错误体全部按入站协议**原样透传**，取消隐式跨协议转换 | ✅ |
| 4 | 出站鉴权：OpenAI 端点 `Authorization: Bearer <key>`；Anthropic 端点 `x-api-key: <key>` + `anthropic-version: 2023-06-01` | ✅ |
| 5 | 同一 Provider 同时配置两套端点时，两条入站路径各自命中对应上游 | ✅ |
| 6 | 多 Provider 故障转移仅在同协议候选之间进行 | ✅ |

### 2.3 严格模式报错【P0】

| # | 功能 | 状态 |
|:-:|------|:----:|
| 1 | 新增错误 `ErrNoProviderForProtocol`；模型绑定的 Provider 均未配置入站协议端点时返回 503 | ✅ |
| 2 | Anthropic 入站错误类型映射为 `overloaded_error`；OpenAI 入站为 `{"code":"ROUTER001","message":...}` | ✅ |
| 3 | 不做协议转换回退（严格模式，与方案决策一致） | ✅ |

---

## 3. ⚠️ 行为变更与升级须知

| 变更 | 影响 | 升级动作 |
|------|------|----------|
| Anthropic 入站不再自动服务「仅配 OpenAI 端点」的 Provider | 此前靠跨协议转换能跑通的 `/v1/messages` 调用，升级后会直接返回 **503 `overloaded_error`** | 上线前为需要支持 `/v1/messages` 的 Provider 补齐 Anthropic 端点，否则先不要切流 |
| 请求体不再跨协议改写 | 上游 Provider 将收到与客户端完全一致的请求体（字段名、`system` 形态等） | 上游必须真实支持该协议的官方契约 |
| 响应体不再跨协议改写 | Anthropic 入站返回 Anthropic 原生响应结构 | 客户端按各自 SDK 正常解析即可（#006/#007 起已支持） |
| 存量 `protocol_type='anthropic'` Provider 的 `base_url` 等列被清空、值搬至 `anthropic_*` | 直接查库/脚本读取 `base_url` 的运维流程会读到空值 | 迁移前先备份 `providers` 表 |
| `binding.api_path_override` 仅对 OpenAI 入站生效 | 双协议 Provider 的 Anthropic 出站路径固定取 `anthropic_api_path` | 无（修复项，避免路径被污染） |

---

## 4. 数据库变更

| 项 | 内容 |
|----|------|
| 文件 | `backend/migrations/20260910_014_add_provider_anthropic_endpoint.up.sql` / `.down.sql` |
| 变更 | `ALTER TABLE providers ADD COLUMN` × 4（`anthropic_base_url` / `anthropic_api_path` 默认 `'/v1/messages'` / `anthropic_api_key_ref` / `anthropic_auth_type` 默认 `'api_key'`，均 `NOT NULL` + 默认值，向后兼容） |
| 数据搬运 | `UPDATE providers SET anthropic_* = base_url... WHERE protocol_type = 'anthropic'`，并清空其 OpenAI 端点列（**不可逆，执行前必须备份 `providers` 表**） |
| 回滚 | `down.sql` 删除 4 列并清理 `schema_migrations` 记录（保证可重新升级）；**不恢复已搬运的端点数据**（需从备份人工恢复） |
| 执行状态 | ✅ **已在真实 PostgreSQL 14.20 实测通过**（2026-09-10，独立库 `nova_migration_test`）：up 存量搬运 / 列默认值 / 外键完整性、down 回滚 / 重新升级 / 备份恢复全部符合预期；过程中修复 1 项 Major（`down.sql` 未清理版本记录）。详见 [测试报告 §3.4](../03-test/TEST-20260910-DualProtocol-Native.md) |

---

## 5. 测试与验证

| 项 | 结果 |
|----|:----:|
| `go build ./...` / `go vet ./...` | ✅ |
| `go test ./...`（含 13 个双协议集成用例） | ✅ 全绿 |
| 真实进程端到端（gateway memory 模式 + mock 上游 + curl，9 步） | ✅ 全部符合预期 |
| 真实 PostgreSQL 迁移（up 存量搬运 + down 回滚 + 重新升级 + 备份恢复） | ✅ 全部符合预期 |
| Admin 前端 `vue-tsc --noEmit` | ✅ |
| Reviewer 审查 | 2 项 Major 已修复（`api_path_override` 污染 Anthropic 路径、迁移 `down.sql` 未清理版本记录），3 项 Minor/Info 已确认符合设计 |

详见 [TEST-20260910-DualProtocol-Native.md](../03-test/TEST-20260910-DualProtocol-Native.md)。

---

## 6. 已知限制

1. **未部署**：迁移 014 已在真实 PostgreSQL 验证通过（本机 14.20 独立库），但**尚未在部署环境执行**；上线前仍须按 §7 备份 `providers` 表并核对搬运结果。
2. **真实厂商端点未实测**：本次为 mock 上游验证，建议生产环境对智谱 / 千问的 OpenAI 与 Anthropic 端点各做一次真实回归（含流式）。
3. **不做协议转换**：这是本迭代的设计取舍（严格模式）；`chat_adapter.go` 转换器代码保留但不再挂载到路由链路，未来如需恢复可加开关。
4. **`anthropic_api_path` 列默认值**：非 Anthropic Provider 该列为 `'/v1/messages'`（因 `anthropic_base_url` 为空而不参与路由）。
5. **图片生成链路未纳入**：`image_service.go` 仍按 OpenAI 兼容端点直连，未做双协议改造（保持原有行为）。
6. **迁移版本号唯一性隐患**：`20260910_014_*` 的 version 前缀解析为 `20260910`，同日第二个迁移会被迁移器静默跳过（已实测复现）。后续迁移请使用 `YYYYMMDDNNNN_` 命名（如 `202609100014_`）；`down.sql` 的 `DELETE FROM schema_migrations` 语句与版本号字符串绑定，重命名迁移文件时需同步更新。

---

## 7. 回滚预案

| 场景 | 操作 |
|------|------|
| 迁移前（必做） | 备份 `providers` 表：`CREATE TABLE providers_bak_YYYYMMDD AS SELECT * FROM providers;`（迁移会清空存量 anthropic Provider 的 `base_url` / `api_path` / `api_key_ref`，不可逆） |
| 迁移异常 | 执行 `20260910_014_...down.sql`（会删除 4 列并清理 `schema_migrations`，使重新升级可正常执行）；已搬运的端点数据需按下方顺序人工恢复 |
| 回滚后重新升级 | 重启 gateway 即由迁移器重新应用 014（`down.sql` 已清理版本记录）；注意：重新升级**不会**自动恢复端点数据，且会以已清空的 `base_url` 覆写 `anthropic_*`，因此**恢复必须在重新升级之后**执行 |
| 端点数据恢复 | `UPDATE providers p SET anthropic_base_url = b.anthropic_base_url, anthropic_api_path = b.anthropic_api_path, anthropic_api_key_ref = b.anthropic_api_key_ref, anthropic_auth_type = b.anthropic_auth_type FROM providers_bak_YYYYMMDD b WHERE p.id = b.id;`（已验证可用） |
| 路由行为异常 | 回退 gateway 二进制至上一版本（无需回滚 DB：新增列可空且旧版本忽略） |
| 客户端 503 激增 | 属预期的严格模式行为，优先为相关 Provider 补配 Anthropic 端点；紧急可先回退 gateway 版本 |
| Admin 表单异常 | 回退 admin 镜像 |

> ⚠️ 回滚禁止手工删除 `schema_migrations` 记录或手工执行 `up.sql`：`up.sql` 非幂等（`ADD COLUMN` 无 `IF NOT EXISTS`），处于「列存在但无版本记录」中间态时迁移器会报 `column "anthropic_base_url" ... already exists` 并退出（已实测复现）。

---

## 8. Change Log

| 日期 | 版本 | 说明 |
|------|------|------|
| 2026-09-10 | v1.0 | 初始归档（Ready for Release，未部署） |
| 2026-09-10 | v1.1 | 补充真实 PostgreSQL 迁移验证结论；修复 `down.sql` 版本记录清理缺陷（Major）；细化回滚预案（备份 → 回滚 → 重新升级 → 人工恢复）；新增迁移命名隐患说明 |

---

# End
